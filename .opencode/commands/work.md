---
description: Start work on a ticket, feature, or bug - always plans first, never jumps to Build
agent: plan
---

We're working on: $ARGUMENTS

Treat the above as the full ask - paste a ticket's title + description here as-is, or describe a
feature/bug inline if there's no ticket.

1. Read `AGENTS.md` before proposing anything.
2. Use the `plan-quality` skill to turn this into a phased plan: what's being done, why, and what
   changes step by step. Small task → a short plan is fine, don't pad it into fake phases.
3. If something's genuinely ambiguous in a way that changes the approach, ask before planning
   further - don't guess past it.
4. Stop here. Don't touch any files - this is Plan mode. I'll switch to Build once the plan looks
   right.
